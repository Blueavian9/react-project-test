{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "react-project-test",
	"main": "./src/worker/index.ts",
	"compatibility_date": "2025-10-08",
	"compatibility_flags": ["nodejs_compat"],
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"assets": {
		"directory": "./dist/client",
		"not_found_handling": "single-page-application"
	}
}

name = "react-project-test"               # ← your Worker name
main = "src/worker.ts"                    # or wherever your Hono entry is (check repo)
compatibility_date = "2025-12-28"         # ← use today's date or later

# We'll add D1 in the next step